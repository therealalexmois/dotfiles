---
paths:
  - "**/*.py"
---

When writing or editing Python code, docstrings, or tests, invoke the `python-conventions` skill first, unless its content is already loaded in this session. It is the source of truth for docstring style (Google style, Russian content) and all test-writing rules.
