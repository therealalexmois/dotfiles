# CS / Python / SQL / DB keep-list

Термины, которые нужно оставлять на английском — устоявшиеся в профессиональном обороте, точного русского эквивалента нет или он режет слух.

## Computer Science (общее)

- `runtime`, `compile time`, `build`
- `stack`, `heap`
- `thread`, `process`, `coroutine`
- `async`, `await`, `event loop`
- `callback`, `hook`
- `cache`, `buffer`, `TTL`
- `lazy`, `eager`
- `immutable`, `mutable`
- `serialization`, `deserialization`
- `encoding`, `decoding`
- `hash`, `checksum`
- `sandbox`
- `scope`, `closure`, `namespace`
- `iterator`, `generator`
- `garbage collection`, `memory leak`
- `deadlock`, `race condition`
- `mutex`, `semaphore`
- `dependency`, `package`, `module`
- `linter`, `formatter`
- `middleware`
- `payload`
- `REST`, `GraphQL`, `gRPC`, `WebSocket`
- `CRUD`
- `idempotent`
- `retry`, `backoff`
- `feature flag`
- `canary`, `blue-green`

## Паттерны проектирования

- `singleton`, `factory`, `observer`, `adapter`, `decorator` (паттерн)
- `repository`, `service`, `use case`
- `DDD`, `CQRS`, `event sourcing`
- `dependency injection`, `IoC`

## Python

- `decorator` (синтаксис `@`)
- `yield`, `list comprehension`, `generator expression`
- `type hint`, `annotation`
- `dataclass`
- `f-string`
- `walrus operator`
- `GIL`
- `virtualenv`, `venv`
- `wheel`, `sdist`
- `fixture` (pytest)
- `mock`, `patch`, `stub`, `spy`
- `mypy`, `ruff`, `pylint`
- `asyncio`
- `Pydantic`
- `pytest`, `parametrize`

## SQL

- `SELECT`, `INSERT`, `UPDATE`, `DELETE`, `UPSERT`
- `JOIN`, `LEFT JOIN`, `INNER JOIN`, `FULL OUTER JOIN`
- `WHERE`, `GROUP BY`, `ORDER BY`, `HAVING`
- `subquery`, `CTE`, `WITH`
- `window function`, `OVER`, `PARTITION BY`
- `EXPLAIN`, `query plan`, `execution plan`
- `index`, `covering index`, `composite index`
- `view`, `materialized view`
- `trigger`
- `constraint` (NOT NULL, UNIQUE, FK, PK)
- `ACID`
- `transaction`, `savepoint`
- `vacuum`

## Базы данных

- `schema`, `migration`
- `primary key`, `foreign key`
- `ORM`
- `connection pool`
- `lock`, `row lock`, `table lock`, `advisory lock`
- `sharding`, `replication`, `replica`
- `hot standby`, `failover`, `failback`
- `WAL` (Write-Ahead Log)
- `CDC` (Change Data Capture)
- `B-tree`, `LSM-tree`
- `OLTP`, `OLAP`
- `ETL`, `ELT`
- `NoSQL`, `document store`, `key-value`, `time-series`
- `backup`, `restore`, `dump`
- `partition`, `table partition`
- `cursor`
- `N+1` (проблема запросов)
