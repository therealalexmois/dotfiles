interface:
  display_name: "Git Worktree"
  short_description: "Управление lifecycle локальных Git worktree"
  default_prompt: "Используй $git-worktree, чтобы безопасно создать, переиспользовать, синхронизировать или удалить Git worktree."
policy:
  allow_implicit_invocation: true
